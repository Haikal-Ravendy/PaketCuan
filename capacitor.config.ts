import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cuan.paketCuan',
  appName: 'PaketCuan',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
